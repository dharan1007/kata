{
  "name": "kata-webmcp",
  "version": "3.0.0",
  "description": "Deterministic research and web interoperability workflows exposed through HTTP, MCP, WebMCP, and a production CLI.",
  "type": "module",
  "license": "MIT",
  "engines": {"node": "24.x"},
  "bin": {"kata": "./bin/kata.mjs"},
  "files": ["bin/", "lib/", "src/", "README.md", "SECURITY.md", "LICENSE"],
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "static-check": "node scripts/static-check.mjs",
    "check": "npm test && npm run build && npm run static-check && node --check bin/kata.mjs && node --check scripts/sbom.mjs",
    "sbom": "node scripts/sbom.mjs",
    "prepack": "npm run build && npm run sbom",
    "verify:package": "npm pack --dry-run"
  }
}
