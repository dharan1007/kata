{
  "name": "kata-webmcp",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22"},
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "static-check": "node scripts/static-check.mjs",
    "check": "npm test && npm run build && npm run static-check"
  }
}
